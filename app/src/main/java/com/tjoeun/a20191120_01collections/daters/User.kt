package com.tjoeun.a20191120_01collections.daters

class User : (name:String ,address) {

    var name = name
    var address = address

    override fun equals(other: Any?): Boolean {
        var result = false

        var otherUser = other as User

        if (this.name == other)
    }
}